---
unit: U11R05
freeze_status: approved_for_freeze
user_approval: confirmed_2026-09-24
---

# 单模型数值诊断判据草案

2026-09-24用户明确“同意”，批准design与本判据。下方待确认措辞为原草案记录；本次按原设置冻结，未改任何门槛或停止规则。

以下是待用户确认的诊断设计，不是已运行结果。不得因U11R04已获批准就自动批准本单元的presolve变化。

## 有效性

冻结完整模型与来源、映射、原始失败记录、CBC二进制及环境版本；模型哈希必须一致。读取原生解必须维度正确、数值有限、日志/退出码/状态一致。

非Optimal、超时或数值异常立即停止，记录blocked/invalid；环境不可用为blocked/null。不得运行第二次补救或调整参数。只允许首次完整通过后进行同配置的独立重复。

以原U11R04单位和闸门核验：权重和与原权重上下界残差<=1e-6；能量、非负辅助变量、偏离约束、目标与lex固定带的违约量<=1e-7；同时独立保存MPS各原始行残差。不把容差内可行说成精确可行，不放宽1e-9固定带。

## 判定

- supported：两次独立求解均Optimal且读回有效，全部变量最大绝对重复差<=1e-6，目标差<=1e-6。只支持该重建失败实例的配置可解性。
- inconclusive：两次各自有效，但重复差超过上述门槛；不调参重做。
- invalid：任一次有效性闸门失败或非Optimal，立即停。
- 未运行：verdict=null。

不改变原1个百分点/2%的科学验收标准；本单元不计算该指标，不得用诊断supported替换U11R04 invalid。源码实现和判据冻结后不得修改；后续改变另立单元。
