---
unit: U11R05
status: complete
verdict: supported
run_ids: [U11R05-v01]
---

# 2026-09-24 当前记录

## 最新执行结果

用户明确“同意”后冻结原方案与输入/映射/原失败记录/CBC二进制。本轮只增加presolve off，其他CBC设置、模型系数、1e-9固定带与验收容差均未改。未修改U11R04源码、结果或判据。

- run_id：U11R05-v01；Windows/Python 3.13.14、PuLP 3.3.0、CBC 2.10.3；执行入口run_cbc_presolve_diagnostic.py。
- 新增src/cbc_presolve_diagnostic.py、独立审计scripts/audit_cbc_presolve_diagnostic.py及tests/test_cbc_presolve_diagnostic.py。原生双精度解验收，文本解只用于状态及舍入差记录。
- 专项5项测试通过（无真实优化调用）；完整70项回归测试通过，回归测试使用的历史小算例求解不计入本单元正式2次调用。
- 正式2次独立CBC进程均Optimal，无失败重试；首次完整读回检查通过后才执行repeat。记录总耗时0.1669681000057608秒，详见manifest；并非完整研究耗时。
- 两次原生目标均为1.0000000002948204（该阶段单个权重w_035，有效天数）；全部变量最大绝对重复差0，目标差0。
- 原单位边界与权重和最大残差0；归一化约束最大残差5.89640994078061e-10，小于冻结1e-7门槛。MPS Decimal逐行最大违约量约5.8964099407806102e-10，非严格零，不能声称精确数学可行。
- 文本目标1.0、文本变量相对原生值最大舍入差2.3690812867016575e-07；正式验收未使用文本变量精度。
- 另进程只读审计：42项冻结/产物哈希通过、源码无漂移、2次状态/命令/原生解/映射/约束读回一致，无新增优化调用。报告results/annual/U11R05-v01-independent-audit.json。

按冻结判据，本诊断为complete/supported，仅支持这个重建失败实例在关闭presolve后可重复求解。相较历史默认presolve on的失败，结果支持配置敏感性的判断；并不证明CBC一般性缺陷或唯一根因。输入是只读重放的MPS，不冒充当时未保存的原临时文件。

完整证据：results/annual/U11R05-v01/first/与repeat/保存模型、命令、日志、退出码、文本/原生解及验证报告；run_manifest.json记录环境、源码/输入/输出哈希与调用数。旧U11R04仍invalid，旧1个百分点/2%研究标准不变，无新年度指标或claims，无PPT。

## 下一步边界

单模型诊断已完成，不能直接将U11R04剩余矩阵补跑并改判。下一研究阶段需提出新的完整矩阵执行方案，预先说明只变更权重LP的presolve设置、保持其他方法/数据/科学判据不变，获确认后另立修订单元冻结验证。当前没有创建或执行该完整矩阵修订。

## 以下为草案历史记录

五份草案已写好，待用户确认。没有freeze.json、实现或优化调用。

提出依据为U11R04只读诊断v02：21阶段LP逐字节一致，上一解残差很小但非严格零，无法确定CBC失败根因。原始U11R04仍invalid。更改presolve属于新的数值实验，不能在旧冻结单元中直接补跑。
