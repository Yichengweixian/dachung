# 执行顺序

1. 用户确认仅关闭presolve的单模型诊断及判据；当前只提交草案。
2. 核对MPS、原模型、映射、原失败记录和CBC版本哈希，冻结；不改U11R04。
3. 在独立实现中保存完整原生求解证据，并测试二进制/映射/残差读取及失败即停。现有FullPrecisionCBC不改写。
4. 首次求解与独立读回全部通过后才重复一次。最多2次优化调用；任何非Optimal立即停止，禁止其他参数扫描。
5. 另进程核验状态、哈希和残差，登记实际结论并提交上传。无完整矩阵、无PPT；不得把诊断完成写成项目研究完成。
