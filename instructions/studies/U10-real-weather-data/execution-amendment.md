# U10 执行前日期和检查细化

2023-01-01至2023-01-07，UTC，武汉30.59/114.30；固定于config/study_sequence_v02.json，不因数据结果换日期。
50m风速单位必须是m/s，辐照度接受W/m^2或Wh/m^2（逐小时总量除1h）；其他单位拒绝。
严格检查完整168个时间戳、UTC元数据、缺失标记、非有限和负值，不插补。
原始JSON字节及HTTP请求参数保存；重复转换另一个Python进程执行，CSV字节一致。
微扰四点：(lat±0.1,lon)、(lat,lon±0.1)，每个风光出力峰值及均值相对基准差严格<10%。
基准零指标仅接受微扰也为零。任一稳健性不通过总体inconclusive，并保留主判据结果。
光伏此单元不封顶（原设计）；后续年度单元封顶必须以U11R02独立规则执行。
判据冻结记录以freeze.json为准，原始draft文件保留不修改。
