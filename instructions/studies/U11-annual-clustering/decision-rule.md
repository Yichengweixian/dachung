---
unit: U11
freeze_status: draft
freeze_commit: null
freeze_sha256: null
---

# U11 判定规则（执行前冻结，冻结后不得修改）

## A. 有效性闸门

- 运行前：全年数据 8760 行完整通过校验；聚类特征、N 候选、种子、储能配置、仿真口径与冻结记录一致。
- 运行后：365 个日 LP 全部 Optimal；聚类两次运行簇分配一致。
- 任一环节失败 → 判 `invalid`。

## B. 主判据（互斥，执行前已定）

- **supported**：存在至少一个 N ∈ {8,10,12}，其聚类加权年度消纳率与全仿真年度消纳率之差 ≤ ±1 个百分点，且弃电率之差 ≤ ±1 个百分点。
- **inconclusive**：全部候选 N 的误差都 > ±1 个百分点（如实记录，说明聚类近似不足，建议增加 N 或换 k-medoids，进 Backlog）。
- **refuted**：聚类结果与全仿真方向性矛盾（如聚类显示消纳率上升而全仿真下降）且无法解释。
- 未执行：verdict 保持 `null`。

## C. 稳健性

- 换种子（42 → 7）重跑聚类，选定的 N 误差仍在 ±1 个百分点内；
- N 相邻档（选定 N±2）误差变化趋势合理（误差随 N 增大不增）。

## D. 论断映射

exploratory，不登记 claims。

## E. 禁止事后修改

冻结后不得修改：特征定义、N 候选、种子、储能配置、仿真口径、误差阈值、判据本身。需要改变 → 新建单元。
