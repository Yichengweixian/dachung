---
unit: U06
freeze_status: draft
freeze_commit: null
freeze_sha256: null
---

# U06 判定规则（执行前冻结，冻结后不得修改）

## A. 有效性闸门

- **运行前**：输入 `data/input_24h.csv` SHA-256、`config/*.json` 快照、本文件 SHA-256 记录在案；现有 25 项测试全过。闸门未满足 → 停止，判 `invalid`。
- **运行后**：求解器状态必须为 Optimal；非 Optimal（Infeasible/Unbounded/超时/异常）→ 停止，判 `invalid`，不得继续绘图或写结论。
- 检验设计失效（判据文件本身被修改、参数与冻结快照不一致、测试脚本与模型实现共用同一代码路径导致"自查自证"）→ 判 `invalid`；模型本身不可行或无解是有效结果，判 `refuted`。

## B. 主判据（互斥，执行前已定）

- **supported**：求解状态 Optimal；全部约束残差 < 1e-6（功率平衡、消纳+弃电=可用、SOC 递推、期末归位、爬坡、上下限）；且与规则基线（U05）的差异可按最小出力/爬坡/期末归位三因素**数值归因**（每个因素的量化影响可独立计算并在 findings 中给出）。
- **refuted**：出现 Infeasible 或其他求解失败（排除输入/参数错误后）；或 Optimal 但约束残差 ≥ 1e-6 且无法解释为求解器数值容差。
- **inconclusive**：求解成功但残差在容差边缘且不可归因；或结果与三因素预期方向矛盾且原因未查明。
- 未执行：verdict 保持 `null`。

## C. 稳健性

以下变体各跑一次，均须满足与主判据相同的残差标准：

1. 时间步长 dt=0.5（输入小时数×2 的等价构造数据，仅验模型，不落盘正式结果）
2. 零储能场景（E=P=0，应与 U02 无储能规则结果可比）
3. 零新能源场景（wind=solar=0，火电独立供负荷，验证最小出力与爬坡约束自身可行）

## D. 论断映射

本单元为 exploratory，**不登记任何 claims**。结果只作为 U07/U09 的上游证据。

## E. 禁止事后修改

冻结后不得修改：输入数据、`config/*.json` 参数、目标函数系数与构成、约束阈值（30/120/30/0.1/0.9/0.5）、判据本身。需要改变 → 新建单元，并在本单元 findings 记录原因。
