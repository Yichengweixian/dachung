---
unit: U13
freeze_status: draft
freeze_commit: null
freeze_sha256: null
---

# U13 判定规则（执行前冻结，冻结后不得修改）

## A. 有效性闸门

- 运行前：U09 有可用网格搜索函数；功能清单与演示算例与冻结记录一致。
- web 层复制调度/成本逻辑 → 判 `invalid`。
- 同输入输出的数字与基准不一致（容差 1e-6 之外）→ 判 `invalid`。

## B. 主判据（互斥，执行前已定）

- **supported**：七模块全部可用；演示算例与基准逐值一致；异常输入有提示不崩溃。
- **refuted**：任一模块不可用或一致性失败且无法解释。
- **inconclusive**：模块可用但一致性仅部分达标（列出差异清单）。
- 未执行：verdict 保持 `null`。

## C. 稳健性

- 换用 input_168h（若 U10 已完成）重跑一致；换浏览器/无头模式页面正常渲染（至少一次人工操作确认）。

## D. 论断映射

exploratory，不登记 claims。

## E. 禁止事后修改

冻结后不得修改：模块清单、演示算例、一致性基准行、判据本身。需要改变 → 新建单元。
