# U11R01执行计划：只修订转换并重新生成年度输入

## 组0 核对与冻结

- [ ] 在实际推进到U11的工作副本工作；识别未提交文件，不用旧Git副本覆盖其进度。
- [ ] 核对原始气象文件、元数据、请求参数、转换代码、旧U11冻结/失败证据和U10历史产物；路径以实际文件为准。
- [ ] 保存基线代码、数据及旧产物哈希。确认输入年份、时区/时间标准、单位、8760×1h时间范围。
- [ ] 按design填写独立转换配置和实现接口，冻结本单元规格与参数，在外部freeze_record.json保存哈希。
- [ ] 如用户本次明确授权实施封顶修订，按上述内容完成必要的组0准备即可继续其授权范围；不为重复确认既定150 MW再提问。尚缺原始单位/数据等必要信息时，只暂停依赖部分并报告具体缺项。

## 组1 实现与转换

1. 按实际代码结构实现显式启用的linear_ac_cap_v1：检查缺失/非数/非法负值和单位；Wh/m²先除以对应小时长度；保持原始数据不变，仅对换算后的可用光伏功率封顶。
2. 读取组0已定稿并冻结的config/u11r01_solar_conversion.json。必要时修改src/convert_weather.py或实际同等模块，但不改变U10旧默认行为，不新装复杂框架。
3. 添加只做转换的scripts/convert_u11_solar_cap.py入口，使用现存年度原始文件；不自动下载替换数据，不调用LP或聚类。
4. 在tests/test_u11_solar_cap.py落实decision-rule的必需测试，独立构造期望值。
5. 执行测试和年度转换，把候选CSV、全时序审计、quality_report和run_manifest写入新的run_id目录。实际结果如何就如何记录，不凑“2小时”。

## 组2 核验与发布

- [ ] 从候选CSV及原始数据独立重算单位转换、上限、封顶次数、时间和损失电量。
- [ ] 比较风电和负荷的同源转换结果，确认本修订只改变光伏输出处理。
- [ ] 验证原始输入、旧冻结记录、旧U10配置与已发布结果的哈希未改变，并完成相关回归测试。
- [ ] 再次执行相同转换，比较数值产物；保留两次run_id，不覆盖第一次记录。
- [ ] 全部通过后发布data/processed/input_8760h.csv；目标若已有文件先归档其字节与哈希，不丢弃历史版本。

检查失败时保存失败日志并停止依赖步骤，不发布正式输入、不继续年度求解。允许在尚未通过的同一单元中修复不改变冻结方法的代码错误并以新run_id重试，旧失败记录完整保留；需要改变方法或参数则另建修订。

## 组3 文档与登记

- [ ] 新建docs/u11_solar_cap_revision.md，写明原始数据未改、交流侧上限假设、单位、实际封顶事件与损失、对后续消纳指标分母的影响。
- [ ] 在本单元findings中按判据登记结果、证据与限制；只在旧U11 findings追加本修订链接，不覆盖旧判定。
- [ ] 向实际工作副本RUNLOG追加记录；依据实际证据更新STATUS和roadmap的本单元状态。不能把这里的旧状态表批量覆盖到已推进的工作副本。
- [ ] 完成后停止。不得自动运行365日调度、聚类或修改其冻结判据；后续年度研究引用新数据哈希另行定稿。

可转交的完整提示词见[05-u11-solar-cap-revision.md](../../prompts/05-u11-solar-cap-revision.md)。
