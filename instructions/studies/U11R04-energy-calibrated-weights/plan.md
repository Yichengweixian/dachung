# U11R04 执行计划

## 组0：当前仅完成草案

- 提交question/design/decision-rule/plan/findings供用户确认。
- 明确本轮只有草案，没有freeze.json、实现或新求解；上传草案不等于批准其方法。
- 待确认重点：只校准权重，不加极端日；权重限定为原簇天数的0.5至2倍；沿用同一N跨三种子的1个百分点与2%标准。

## 组1：用户确认后冻结

- 核对原基准、U11R03与运行环境，记录完整输入及规则SHA-256和Git提交。
- 生成新单元freeze.json，不改旧冻结文件。冻结前异常只报告，不进入新实验。

## 组2：冻结后实现与测试

- 新增独立权重模块、运行入口与审计入口（具体路径在实现前登记），复用现有聚类和调度，不修改其历史行为。
- 实现非负受限权重LP、层级目标与确定性平局规则，保留每次调用状态。
- 按decision-rule C加入边界与失败测试，再运行全量unittest。

## 组3：执行与独立审计

- 新建不覆盖的results/annual/U11R04-v01运行目录，使用StudyRun记录环境、Git版本、输入/输出哈希和真实调用数。
- 按冻结顺序完成9组；非Optimal立即停止，保存完整失败记录。
- 保存原簇天数、校准权重、原始日曲线、LP阶段证据、逐时调度、年度汇总、对照与判定。
- 另进程审计，逐项重建结果。通过不等于跨年度验证。

## 组4：登记与发布

- 按实际结果更新本单元findings、RUNLOG、STATUS、顺序台账与roadmap；保留历史inconclusive。
- 结果材料同步另行安排；用户明确不需要PPT，本单元不制作PPT。
- 依用户持续授权提交并推送GitHub。完成本单元后停止，不自行转入极端日或跨日模型研究。
