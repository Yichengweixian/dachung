# 执行细化（运行前冻结）

初始30个均匀随机个体，独立种子42/7/2026。锦标赛大小2；每代1个精英原样保留且不重新评估，其余29个由交叉与变异生成。完整执行50代，每种子真实评估30+29×50=1480次，不提前停止、不缓存、不重复计数精英。
使用有界SBX和有界多项式变异，确保变量不出定义域。每代候选按生成顺序以最多4个工作线程并发调用相同的evaluate_candidate；CBC各自独立串行进程，结果按原顺序登记，不按完成先后改变选择。
三个种子依次运行。评估次数为主比较量；并发与串行环境不同，墙钟时间仅记录，不据此声称算法加速。
所有3种子误差都>=0.5%则refuted；其余不满足supported的情况为inconclusive。三种子极差和总体标准差均须<网格成本0.5%才通过种子稳健性。异常求解立即记录并停止，不把失败当作有效评估。
