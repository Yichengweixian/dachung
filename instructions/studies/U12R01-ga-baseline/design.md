# U12R01 设计

沿用种群30，50代，种子42/7/2026；实数锦标赛、SBX eta=15概率0.9、多项式变异eta=20每基因概率0.5，精英1。E在[0,100],P在[5,50]；E<5映射(0,0)。不复用网格答案初始化；无跨运行缓存，每次调用目标计数。

只用master本地代码，所有结果区分构造输入/气象估计与实测。运行先冻结，代码与依赖版本、输入哈希均入manifest。

