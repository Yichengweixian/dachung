# U06R01 设计

复用原LP建模函数并新增显式mutual_exclusion开关，默认关闭；修订运行开启。CBC原生双精度解读回，维度/字节数/目标核对。所有原阈值保持1e-6。主场景与规则基线的差异先用同约束优化与顺序约束路径区分；U07R01负责全组合归因。

只用master本地代码，所有结果区分构造输入/气象估计与实测。运行先冻结，代码与依赖版本、输入哈希均入manifest。

