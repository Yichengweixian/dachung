# U00 / T1 非提交补证边界

用户确认本轮仅执行T1，不提交、不推送、不启动后续修订实验。原decision-rule.md与plan.md原样冻结，不以本说明替代或放宽原判据。

本轮交付新增清单脚本、现有文件快照、results产物归属索引、Python/pip/Git环境、全量测试和PowerShell/Git Bash核对记录，存入新的results/baseline/U00-T1-v01/目录。
仅进行工程补证，不重算历史研究、不修改旧run目录。历史判定按manifest及findings分别保留，不把人工验收后的判定回写机器运行。

清单采用采集时点语义：按原排除目录集合枚举现有文件，之后新建本轮输出目录；第二遍重新枚举时只扣除本次新增的输出文件。新产物无法同时记录自身最终哈希，输出哈希另由run_manifest记录，不声称实现包含自身哈希的固定点。未来新增或修改文件应报告差异，不覆盖旧清单。

剩余正式验收项：原方案的工作区基线提交本次不做；444.docx已被既有HEAD跟踪，本轮不改动其入库状态，但不能代替原流程要求的用户确认。原方案的自包含清单与既定输出路径存在自引用问题，需确认口径后才能完整验收；本轮仅报告辅助检查，不将U00改判为supported。
测试命令保持python -m unittest discover -s tests -p "test_*.py" -v；原有25项之外的新测试一并执行，不删除测试以匹配旧数量。
