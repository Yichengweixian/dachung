---
unit: U11R07
status: complete
verdict: inconclusive
run_ids: [U11R07-v01]
---

# U11R07 只读诊断结果（2026-09-26）

按冻结规则核对 U11R06 失败现场，无新增优化调用。首次预检因复制诊断脚本时残留 `range(48)`，在读取 24 个代表日前报 `IndexError`；当时没有重放模型、调用 CBC 或写入诊断文件，输出目录为空。修正为 `range(len(reps))` 后在同一空目录完成正式诊断；没有更改冻结方法、原结果或求解器设置。

重放至 `N24_seed42_main_lex_014` 共 16 阶段，全部重放 LP 与 U11R06 原保存文件 SHA-256 一致。诊断中禁用了真实 CBC 类并拦截 subprocess 调用，新增求解次数 0。失败阶段有 13 个权重固定带，最窄交集约 1.00000008e-9；仅从单变量上下界与总权重看，下界和 291.04419023983064、上界和 591.04419026378，包含 365，没有简单区间矛盾。

前一阶段 `lex_006` 的已保存解代入失败阶段，内存模型最大违约量约 2.9979974059e-9，重新导出 MPS 最大违约量约 2.9980008882e-9。最大项是旧解的 `w_010` 高于下一阶段固定上界（LP 行 `_C56`，MPS 行 `C0000058`）。原生双精度模型系数到 MPS 最大差约 3.3343e-14，RHS 最大差约 4.5563e-12。旧解并非下一阶段的严格可行见证；仅凭小残差也不能证明新阶段数学上无解，更不能确认 CBC 缺陷或唯一根因。重建 MPS 不是当时未保存的原临时 MPS。

独立另进程审计 `results/annual/U11R07-v01-independent-audit.json` 通过：26 项冻结哈希、120 项 U11R06 产物哈希、16 个 LP 文件、诊断源码与输出哈希一致，Decimal MPS 残差读回一致。75 项全量测试通过。本单元工程 complete，但数值根因未证实，verdict=inconclusive。U11R06 blocked/invalid 和 U11R05 单模型 supported 不改变；下一步若改算法、固定带或求解器参数，须新单元先定判据。
