---
unit: U08
freeze_status: draft
freeze_commit: null
freeze_sha256: null
---

# U08 判定规则（执行前冻结，冻结后不得修改）

## A. 有效性闸门

- 运行前：U07 verdict = supported；场景表、输入、参数与冻结记录一致。
- 运行后：12 场景求解状态全部 Optimal；任一非 Optimal → 停止，判 `invalid`（排除后仅重跑该场景，不修参数凑解）。
- 检查脚本复用 U06/U07 的独立实现，不新写"宽松版"检查。

## B. 主判据（互斥，执行前已定）

- **supported**：12 场景约束检查全过，且以下三点全部成立：①弃电率随系数 0.8→1.4 单调不减；②同一系数下消纳率随储能容量单调不减；③40→80 的消纳率增益 ≤ 0→40 的增益（边际不增）。
- **refuted**：①或②出现反例且原因无法解释。
- **inconclusive**：③不成立但①、②成立（说明边际递减在 24h 构造数据上未显现，如实记录）；或趋势成立但存在异常场景未查明。
- 未执行：verdict 保持 `null`。

## C. 稳健性

- 系数不变、仅重跑一次全部场景，结果逐列一致（确定性求解）；
- 边界场景（R140_E000）如出现失负荷，其失负荷量与"火电上限 120 + 风光可用"手算核对。

## D. 论断映射

exploratory，不登记 claims。

## E. 禁止事后修改

冻结后不得修改：系数集合、储能集合、场景命名、检查阈值、判据本身。需要改变 → 新建单元。
