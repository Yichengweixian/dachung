# 预算方法草案（待确认）

- 数据：data/input_24h.csv；费用、约束、变量域沿用U09R01-v01和U12R01-v01的冻结配置。冻结前确认双方相同，不修改原始网格。
- 基准：results/grid_search/U09R01-v01/summary.csv的101候选最优，核对manifest及保存结果后从CSV选取，不手填成本。
- 目标：复用evaluate_candidate及其共同的互斥MILP、经济核算。E∈[0,100]MWh、P∈[5,50]MW；E<5映射(0,0)。连续域与离散域差异明确保留。
- 种群12，初始均匀随机；完整7代，每代保留1精英不重算，生成11子代，总调用12+7×11=89。不提前停止，不使用网格最优值/解引导初始化、选择或终止。
- 其余算子沿用U12R01：大小2锦标赛、有界SBX eta=15/概率0.9、有界多项式变异eta=20/每基因概率0.5；无跨运行或运行内目标缓存，重复候选照常求解并计数。
- 10个固定种子：[42,7,2026,11,23,101,314,509,1024,4096]。每种子一个主运行；另独立重复种子42一次核对轨迹，不作为第11个独立统计样本。
- 所有目标调用串行，CBC threads=0。目标调用顺序固定。主运行计划890次，复现检查另89次，合计979次，不把检验开销隐藏在“每次89”之外。
- 保存每次候选、调用序号、目标值、最优轨迹、逐时结果和求解日志。非Optimal立即停止，不能用罚值替代失败并算作有效评估。

89是运行前提议的固定预算，较101少12次（12/101，约11.88%）；不是读取历史收敛曲线后选择的首次命中点。
只检验每次试验目标调用数与解质量，不声称本次全部研究开销少于单次101点网格，也不声称墙钟加速或连续全局最优。
