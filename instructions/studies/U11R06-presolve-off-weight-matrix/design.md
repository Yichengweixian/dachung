# U11R06 设计

完整继承 U11R04 的输入、基准、聚类日集合、N=[12,24,48]、seed=[42,7,2026]、权重上下界 0.5c 至 2c、sum(w)=365、三层 LP 层级及 1e-9 固定带、调度 MILP、复算与导出方法。唯一改变：权重 LP 的 CBC 命令在 `-branch` 前加入 `-presolve off`。调度 MILP 求解器配置不变。不改变数据、物理模型、失败处理或研究门槛。

U11R05-v01 为单模型可行性的先行证据，不计入本单元 540 LP/252 MILP 预算。新模块、运行入口及审计入口分别独立于 U11R04，旧代码与产物只读。沿用 U11R04-isolated-preflight-v02 的历史基准重建证据并核对哈希。报告同年度样本内输入拟合，不称跨年或独立验证。
